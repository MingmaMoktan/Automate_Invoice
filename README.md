# Invoice Automation SaaS

A production-grade Django application that automates invoice data extraction using OCR (Tesseract) and provides a dashboard for financial management.

## 🚀 Quick Start (Local Setup)

### Prerequisites
- Python 3.10+
- Tesseract OCR engine (`sudo apt install tesseract-ocr`)
- WSL2 (Ubuntu 22.04)

### Installation
1. **Clone the repo:**
   ```bash
   git clone https://github.com/MingmaMoktan/Automate_Invoice.git